pragma ton-solidity >= 0.57.0;

import "tip3/contracts/interfaces/ITokenRoot.sol";

interface IVoteTokenRoot is ITokenRoot{
    function getTotalSupply() external view responsible returns (uint256);
    function getPastTotalSupply(uint256 blockTimestamp) external view responsible returns (uint256);
}